Paste the NIGHTLY variant ASCII art here.

This replaces stable-current.txt's content when BuildInfo.isNightly() is true,
once the swap mechanism is built. Paste raw, exactly as you want it to appear
as `#`-prefixed YAML comment lines in config.yml — same format as
stable-current.txt in this folder (each line will be treated literally, so
alignment/spacing matters).

Delete this placeholder text before pasting.
